# Test Environment Configuration
NODE_ENV=test

# Auth Configuration for Testing
AUTH_SECRET=test-secret-key-for-playwright-testing-only
AUTH_URL=http://localhost:3000/api/auth

# Database Configuration (use test database)
DATABASE_URL=postgresql://postgres:password@localhost:5432/media_contacts_test

# Disable external services in tests
SKIP_ENV_VALIDATION=true

# Test-specific settings
NEXTAUTH_URL=http://localhost:3000
