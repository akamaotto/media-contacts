// Placeholder mock data for Prisma seed script.
// These arrays intentionally start empty to avoid unintended data seeding.
// Populate locally as needed for development.

export const mockOutlets: Array<{ id: string; name: string; description?: string | null; website?: string | null }> = [];

export const mockBeats: Array<{ id: string; name: string; description?: string | null }> = [];

// Structure kept flexible to match seed script usage (connect syntax, etc.)
export const mockMediaContacts: Array<any> = [];
